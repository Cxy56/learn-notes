{
  "scripts": {
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs",
    "deploy": "gh-pages -d docs/.vuepress/dist",
    "build:deploy": "npm run docs:build && npm run deploy"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "vuepress": "^1.9.7"
  }
}
